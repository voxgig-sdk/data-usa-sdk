<?php
declare(strict_types=1);

// DataUsa SDK utility: feature_hook

class DataUsaFeatureHook
{
    public static function call(DataUsaContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
